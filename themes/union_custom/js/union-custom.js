/**
 * @file
 * Union Custom behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.unionCustom = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
